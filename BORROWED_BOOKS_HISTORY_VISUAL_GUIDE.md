# Visual Guide: New Features in BorrowedBooks & BorrowHistory

## Component Layout Diagrams

### BorrowHistory Component

```
┌─────────────────────────────────────────────────────────────────┐
│  🕐 Lịch sử mượn sách                                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [ Tất cả ] [ Chờ duyệt ] [ Đã duyệt ] [ Đã trả ]  ← Status tabs
│                                                                 │
│  ┌─────────────────┬────────────┬────────────┬──────────────┐  │
│  │ 🔍 Tìm kiếm...  │ Từ ngày    │ Đến ngày   │ [Xóa lọc]   │  │
│  └─────────────────┴────────────┴────────────┴──────────────┘  │
│                                                                 │
│  ┌──────────────────────────────┬──────────────────────────┐   │
│  │ Sort: [Ngày mượn (Mới nhất)] │    [📥 Xuất CSV]        │   │
│  └──────────────────────────────┴──────────────────────────┘   │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ Tên sách │ Tác giả │ SL │ Ngày mượn │ Hạn trả │ ... │    │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │ Book 1   │ Author  │ 2  │ 01/12/24  │ 08/12/24│ ... │    │ │
│  │ Book 2   │ Author  │ 1  │ 30/11/24  │ 07/12/24│ ... │    │ │
│  │ ...      │ ...     │... │ ...       │ ...     │ ... │    │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│         [ ◀ ] [ 1 ] [ 2 ] [ 3 ] [ 4 ] [ 5 ] [ ▶ ]   ← Pagination
│                                                                 │
│         Hiển thị 1 - 10 trong tổng số 47 bản ghi               │
│                                                                 │
│  ┌──────────┬──────────┬──────────┬──────────┐                 │
│  │    47    │    5     │    12    │    30    │  ← Statistics   │
│  │  Tổng    │ Chờ duyệt│ Đã duyệt │  Đã trả  │                 │
│  └──────────┴──────────┴──────────┴──────────┘                 │
└─────────────────────────────────────────────────────────────────┘
```

### BorrowedBooks Component

```
┌─────────────────────────────────────────────────────────────────┐
│  📚 Sách đã mượn                                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────┬──────────────┬─────────────────────┐     │
│  │ 🔍 Tìm kiếm...   │ [Trạng thái] │ [Sort: Ngày mượn]  │     │
│  └──────────────────┴──────────────┴─────────────────────┘     │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ Tên sách │ Tác giả │ SL │ Ngày mượn │ ... │ Thao tác    │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │ Book 1   │ Author  │ 2  │ 01/12/24  │ ... │ [👁][🔄]    │ │ ← Approved
│  │ Book 2   │ Author  │ 1  │ 30/11/24  │ ... │ [👁][❌]    │ │ ← Pending
│  │ ...      │ ...     │... │ ...       │ ... │ ...        │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│         [ ◀ ] [ 1 ] [ 2 ] [ 3 ] [ ▶ ]          ← Pagination   │
│                                                                 │
│         Hiển thị 1 - 10 trong tổng số 15 bản ghi               │
│                                                                 │
│  ℹ️ Tổng số sách đang mượn: 15 | Quá hạn: 2                   │
└─────────────────────────────────────────────────────────────────┘

Legend:
👁 = Xem chi tiết (View Detail)
🔄 = Mượn lại (Borrow Again) - Shows for approved books
❌ = Hủy yêu cầu (Cancel Request) - Shows for pending books
```

---

## Feature Interaction Flow

### Search Flow

```
User types in search box
        ↓
searchQuery.value updates (reactive)
        ↓
filteredHistory/activeBorrows computed property recalculates
        ↓
sortedAndFilteredHistory/sortedActiveBorrows updates
        ↓
paginatedHistory/paginatedBorrows updates
        ↓
Table re-renders with filtered results
        ↓
Pagination adjusts to new result count
```

### Filter & Sort Flow (BorrowHistory)

```
User selects status tab
        ↓
filter.value changes
        ↓
User types search query
        ↓
searchQuery.value updates
        ↓
User selects date range
        ↓
dateFrom/dateTo values update
        ↓
User selects sort option
        ↓
sortBy.value changes
        ↓
All computed properties cascade update
        ↓
Table shows final filtered & sorted results
```

### Pagination Flow

```
User clicks page number (e.g., page 3)
        ↓
changePage(3) called
        ↓
currentPage.value = 3
        ↓
paginatedHistory/paginatedBorrows recalculates
        ↓
slice(20, 30) applied to sorted data
        ↓
Table shows items 21-30
        ↓
Page indicator updates
        ↓
"Hiển thị 21 - 30..." updates
```

### Cancel Request Flow (BorrowedBooks)

```
User clicks ❌ on pending book
        ↓
cancelBorrowRequest(borrowId) called
        ↓
Confirmation dialog appears: "Bạn có chắc chắn...?"
        ↓
User clicks "OK"
        ↓
API call: DELETE /muonsach/delete-muonsach/:id
        ↓
Backend deletes the borrow request
        ↓
Success alert: "Đã hủy yêu cầu..."
        ↓
loadBorrowedBooks() called
        ↓
Fresh data fetched from API
        ↓
Table updates, request removed
```

### CSV Export Flow (BorrowHistory)

```
User clicks "Xuất CSV"
        ↓
exportToCSV() called
        ↓
Headers array created
        ↓
Current filtered/sorted data mapped to rows
        ↓
CSV content built with BOM (\uFEFF)
        ↓
Blob created with UTF-8 encoding
        ↓
Download link created
        ↓
Filename: "lich-su-muon-sach-2024-12-05.csv"
        ↓
File downloads to user's computer
```

---

## State Management Diagram

### BorrowHistory.vue

```
┌─────────────────────────────────────────────────────────────┐
│                     REACTIVE STATE                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  history: ref<TheoDoiMuonSach[]>([])      ← Raw data       │
│  filter: ref('all')                        ← Status filter  │
│  searchQuery: ref('')                      ← Search input   │
│  dateFrom: ref('')                         ← Date filter    │
│  dateTo: ref('')                           ← Date filter    │
│  sortBy: ref('date_desc')                  ← Sort option   │
│  currentPage: ref(1)                       ← Page number    │
│  itemsPerPage: ref(10)                     ← Page size      │
│  isLoading: ref(false)                     ← Loading state  │
│  errorMessage: ref('')                     ← Error state    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│                   COMPUTED PROPERTIES                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  filteredHistory          ← Filter by status, search, dates│
│       ↓                                                     │
│  sortedAndFilteredHistory ← Apply sorting                  │
│       ↓                                                     │
│  paginatedHistory         ← Slice for current page         │
│                                                             │
│  totalPages               ← Calculate total pages          │
│  displayPages             ← Calculate visible page numbers │
│  statistics               ← Calculate counts by status     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### BorrowedBooks.vue

```
┌─────────────────────────────────────────────────────────────┐
│                     REACTIVE STATE                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  borrows: ref<TheoDoiMuonSach[]>([])      ← Raw data       │
│  searchQuery: ref('')                      ← Search input   │
│  statusFilter: ref('all')                  ← Status filter  │
│  sortBy: ref('date_desc')                  ← Sort option   │
│  currentPage: ref(1)                       ← Page number    │
│  itemsPerPage: ref(10)                     ← Page size      │
│  showBorrowModal: ref(false)               ← Modal state    │
│  selectedBook: ref<Sach | null>(null)      ← Selected book  │
│  borrowQuantity: ref(1)                    ← Borrow qty     │
│  isBorrowing: ref(false)                   ← Submit state   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│                   COMPUTED PROPERTIES                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  activeBorrows            ← Filter not returned + search +  │
│       ↓                      status                         │
│  sortedActiveBorrows      ← Apply sorting                  │
│       ↓                                                     │
│  paginatedBorrows         ← Slice for current page         │
│                                                             │
│  totalPages               ← Calculate total pages          │
│  displayPages             ← Calculate visible page numbers │
│  overdueCount             ← Count overdue books            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## Button States Diagram

### Action Buttons in BorrowedBooks

```
Book Status: "Chờ duyệt" (Pending)
┌────────────────────────────────┐
│  [👁 View]  [❌ Cancel]       │
│   Always     Only pending      │
└────────────────────────────────┘

Book Status: "Đã duyệt" (Approved)
┌────────────────────────────────┐
│  [👁 View]  [🔄 Borrow Again] │
│   Always     Only approved     │
│                                │
│  Disabled if SoQuyen === 0     │
└────────────────────────────────┘

Book Status: "Đã trả" (Returned)
┌────────────────────────────────┐
│  Filtered out (not shown in    │
│  BorrowedBooks, only in        │
│  BorrowHistory)                │
└────────────────────────────────┘
```

---

## Pagination Visual States

### Page 1 of 5

```
[ ◀ ]  [ 1 ]  [ 2 ]  [ 3 ]  [ 4 ]  [ 5 ]  [ ▶ ]
  ↑     ↑
  disabled  active
```

### Page 3 of 5

```
[ ◀ ]  [ 1 ]  [ 2 ]  [ 3 ]  [ 4 ]  [ 5 ]  [ ▶ ]
             ↑
           active
```

### Page 5 of 5

```
[ ◀ ]  [ 1 ]  [ 2 ]  [ 3 ]  [ 4 ]  [ 5 ]  [ ▶ ]
                                    ↑       ↑
                                  active  disabled
```

### Page 10 of 20 (shows 5 page numbers)

```
[ ◀ ]  [ 8 ]  [ 9 ]  [ 10 ]  [ 11 ]  [ 12 ]  [ ▶ ]
                      ↑
                    active
```

---

## Data Flow Architecture

```
┌─────────────┐
│   User UI   │
└──────┬──────┘
       │ User actions (search, filter, sort, paginate)
       ↓
┌──────────────────────┐
│  Vue Components      │
│  - BorrowHistory     │
│  - BorrowedBooks     │
└──────┬───────────────┘
       │ Reactive state updates
       ↓
┌──────────────────────┐
│ Computed Properties  │
│ - Filter pipeline    │
│ - Sort logic         │
│ - Pagination slice   │
└──────┬───────────────┘
       │ Derived data
       ↓
┌──────────────────────┐
│   Template/DOM       │
│   - Tables           │
│   - Pagination UI    │
│   - Stats display    │
└──────────────────────┘

For API calls:
┌─────────────┐
│  Component  │
└──────┬──────┘
       │ Call service method
       ↓
┌──────────────────────┐
│  bookService         │
│  - getBorrowHistory  │
│  - cancelBorrowReq   │
└──────┬───────────────┘
       │ HTTP request
       ↓
┌──────────────────────┐
│  Backend API         │
│  - Express routes    │
│  - Controllers       │
│  - Services          │
└──────┬───────────────┘
       │ Database operation
       ↓
┌──────────────────────┐
│    MongoDB           │
└──────────────────────┘
```

---

## Filter Pipeline Visualization

### BorrowHistory Filter Pipeline

```
Input: All history records (100 items)
         ↓
Step 1: Status Tab Filter
         - Filter: "Đã duyệt"
         - Result: 40 items
         ↓
Step 2: Search Filter
         - Query: "harry"
         - Result: 5 items
         ↓
Step 3: Date Range Filter
         - From: 2024-11-01
         - To: 2024-11-30
         - Result: 3 items
         ↓
Step 4: Sort
         - By: fine_desc
         - Result: 3 items (reordered)
         ↓
Step 5: Pagination
         - Page: 1
         - Items per page: 10
         - Result: 3 items (all fit on page 1)
         ↓
Output: Display 3 items in table
```

---

## CSV Export Structure

```
File: lich-su-muon-sach-2024-12-05.csv

┌────────────────────────────────────────────────────────┐
│ BOM (\uFEFF) ← For UTF-8 encoding                     │
├────────────────────────────────────────────────────────┤
│ "Tên sách","Tác giả","Số lượng","Ngày mượn",...       │
├────────────────────────────────────────────────────────┤
│ "Harry Potter","J.K. Rowling","2","05/12/2024",...    │
│ "Nhà Giả Kim","Paulo Coelho","1","04/12/2024",...     │
│ ...                                                    │
└────────────────────────────────────────────────────────┘

Each cell is quoted to handle commas in content
Vietnamese characters are preserved with BOM
```

---

## Responsive Breakpoints

### Desktop (> 768px)

```
┌──────────────────────────────────────────────────────┐
│  [Search Input (33%)]  [Date From]  [Date To]  [Clear] │
│  [Sort (33%)]          [Export CSV (align right)]     │
│                                                        │
│  [Table with all columns visible]                     │
└──────────────────────────────────────────────────────┘
```

### Mobile (< 768px)

```
┌────────────────────────┐
│  [Search Input (100%)] │
│  [Date From (100%)]    │
│  [Date To (100%)]      │
│  [Clear (100%)]        │
│  [Sort (100%)]         │
│  [Export CSV (100%)]   │
│                        │
│  [Table scrolls →]     │
└────────────────────────┘
```

---

## Error States

### No Results Found

```
┌─────────────────────────────────────┐
│  🔍 Search: "xyz"                   │
│  📅 From: 2024-01-01                │
│  📅 To: 2024-01-31                  │
├─────────────────────────────────────┤
│                                     │
│         📭                          │
│    Không tìm thấy kết quả          │
│                                     │
│    [Xóa lọc để xem tất cả]         │
│                                     │
└─────────────────────────────────────┘
```

### Loading State

```
┌─────────────────────────────────────┐
│                                     │
│           ⏳                        │
│     Đang tải dữ liệu...            │
│                                     │
└─────────────────────────────────────┘
```

### Empty State (No Borrows)

```
┌─────────────────────────────────────┐
│                                     │
│           📚                        │
│    Bạn chưa mượn sách nào          │
│                                     │
│    [Tìm sách để mượn]              │
│                                     │
└─────────────────────────────────────┘
```

---

## Confirmation Dialogs

### Cancel Borrow Request

```
┌─────────────────────────────────────────┐
│  ⚠️ Xác nhận                            │
├─────────────────────────────────────────┤
│                                         │
│  Bạn có chắc chắn muốn hủy yêu cầu     │
│  mượn sách này?                        │
│                                         │
│           [Hủy]      [OK]              │
└─────────────────────────────────────────┘
```

### Success Alert

```
┌─────────────────────────────────────────┐
│  ✅ Thành công                          │
├─────────────────────────────────────────┤
│                                         │
│  Đã hủy yêu cầu mượn sách thành công   │
│                                         │
│                [OK]                     │
└─────────────────────────────────────────┘
```

---

## Performance Optimization Points

```
Original Implementation (without features):
- Renders all 100+ items at once
- No filtering, manual scrolling needed
- Hard to find specific records

New Implementation (with features):
- Renders only 10 items per page ✓
- Client-side filtering (fast) ✓
- Efficient computed property caching ✓
- Minimal re-renders ✓

Potential future optimizations:
- Add search debouncing (300ms delay)
- Implement virtual scrolling for very large lists
- Add request caching with cache invalidation
- Lazy load images if book covers added
```

---

This visual guide provides a comprehensive overview of how the new features work together to create an enhanced user experience!

